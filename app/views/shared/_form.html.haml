= form_for(@article) do |f|
	%ul
		= @article.errors.full_messages.each do |errors|
			%li= errors

	%p
		= f.label :title
		= f.text_field :title
	%p
		= f.label :body
		= f.text_field :body
	%p
		= f.label :tag_list
		= f.text_field :tag_list
	%p
		= f.submit
